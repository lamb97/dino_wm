name: droid_pth
args: []
kwargs: {}

dataset:
  _target_: "datasets.droid_pth_dset.load_droid_pth_slice_train_val"
  n_rollout: null
  normalize_action: ${normalize_action}
  data_path: ${oc.env:DATASET_DIR}/droid_pth_ext1
  split_ratio: 0.9
  max_slices_per_traj: null
  transform:
    _target_: "datasets.img_transforms.default_transform"
    img_size: ${img_size}

decoder_path: null
num_workers: 4
